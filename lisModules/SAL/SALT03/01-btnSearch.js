export default async function (dv) {
  dv.reportList = await lissaldocs.find({
    company: dv.sc.company,
    name1: lis.like(dv.sc.name1),
  });

  String;
  Number;
  Boolean;
  Date;

  var salih = 5;

  salih = "batmaz";

  const salih = 5;

  let salih = 5;

  let benımsayım = array[i];

  for (let i in array) {
    benımsayım = array[i];
  }

  return dv;
}
