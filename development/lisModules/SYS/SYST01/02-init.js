export default async function (dv) {
  if (dv.modi == 0) {
  }

  if (dv.modi == 1) {
    dv.lisappobjects = new lisappobjects();
  }

  if (dv.modi == 2) {
    dv.lisappobjects = new lisappobjects();
  }

  return dv;
}
